{
  "name": "mobile-utils-shared",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/mobile/utils/shared/src",
  "projectType": "library",
  "tags": [],
  "// targets": "to see all targets run: nx show project mobile-utils-shared --web",
  "targets": {
    "check-types": {
      "executor": "nx:run-commands",
      "outputs": [],
      "options": {
        "cwd": "libs/mobile/utils/shared",
        "command": "npx tsc -p tsconfig.lib.json --noEmit --skipLibCheck && npx tsc -p tsconfig.spec.json --noEmit --skipLibCheck"
      }
    }
  }
}
